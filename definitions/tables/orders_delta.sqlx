config {
    type : "incremental",
    schema : "ankit_staging",
    uniqueKey: ["OrderID"],
    tags : ["something"]
}

select * from ${ref("orders")}
${when(incremental(), `WHERE OrderDate > (SELECT MAX(OrderDate) FROM ${self()})`) }